# 10. 중복 제거하기

![mysql_ct10](/Users/choibyeonghwi/Desktop/스크린샷/코딩테스트/mysql_ct10.png)

```mysql
SELECT COUNT(DISTINCT NAME) AS 'count'
FROM ANIMAL_INS;
```

* 중복되는 행을 제거하기 위해서는 `DISTINCT`라는 옵션이 필요하다.